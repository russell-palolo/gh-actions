name: Setup

description: "Setup the database for other workflows"

runs:
  using: "composite"

  steps:
    - name: Run a one-line script
      run: |
        echo Hello, world!
        sleep 1
      shell: bash
